[phases.build]
cmds = ["npm ci", "npm run build", "composer install --no-dev --optimize-autoloader"]

[start]
cmd = "php artisan migrate --force && php artisan view:clear && php artisan cache:clear && php artisan storage:link && php artisan config:cache && php artisan serve --host=0.0.0.0 --port=$PORT"
